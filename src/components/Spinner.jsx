import spinner from "../img/spinner.gif";

const Spinner = () => {
	return (
		<img style={{
			margin: "0 auto",
			width: "190px",
			height: "190px"
		}}src={spinner} alt="loading" />
	)
}

export default Spinner;
